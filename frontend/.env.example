# Copy to .env.local. Without a real app id the app runs with a designed
# setup notice — identity verification and the dev test-key path still work.
# Create a free app at https://dashboard.privy.io (free under 499 MAU).
VITE_PRIVY_APP_ID=
